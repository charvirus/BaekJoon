#include <iostream>
using namespace std;
int m;

int main(void) {
    ios::sync_with_stdio(false), cin.tie(NULL);
    cin >> m;
    for (int i = 0;i<m;i++) {
        cout<<"I love DGU\n";
    }
}
