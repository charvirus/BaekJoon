#include <cmath>
#include <cstdint>
#include <iostream>
using namespace std;


int main(void) {
    ios::sync_with_stdio(false), cin.tie(NULL);

    double n;
    cin >> n;
    cout.setf(ios::showpoint);
    cout<< n - 0.3;
}
